version: "2"

linters:
  default: standard
  enable:
    - unconvert
    - misspell
  settings:
    errcheck:
      exclude-functions:
        - (io.Closer).Close
        - (*database/sql.DB).Close

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
